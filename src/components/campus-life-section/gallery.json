{
  "collectionName": "components_campus_life_section_galleries",
  "info": {
    "displayName": "gallery",
    "description": "One tab's photo set. The first image doubles as that tab's backdrop."
  },
  "options": {},
  "attributes": {
    "group": {
      "type": "enumeration",
      "enum": ["infrastructure", "ai-labs", "events", "fests", "academic"],
      "required": true
    },
    "images": { "type": "media", "multiple": true, "required": true, "allowedTypes": ["images"] }
  }
}
