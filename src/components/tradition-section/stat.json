{
  "collectionName": "components_tradition_section_stats",
  "info": {
    "displayName": "stat",
    "description": "One counter tile in the stats grid."
  },
  "options": {},
  "attributes": {
    "value": { "type": "string", "required": true, "maxLength": 20 },
    "label": { "type": "string", "required": true, "maxLength": 30 },
    "note": { "type": "string", "required": false, "maxLength": 120 }
  }
}
