{
  "collectionName": "components_innovation_startups_section_stats",
  "info": { "displayName": "stat", "description": "One tile in the ecosystem bento." },
  "options": {},
  "attributes": {
    "value": { "type": "string", "required": true, "maxLength": 20 },
    "prefix": { "type": "string", "required": false, "maxLength": 4 },
    "label": { "type": "string", "required": true, "maxLength": 60 },
    "tone": {
      "type": "enumeration",
      "enum": ["ink", "red", "track", "plain"],
      "default": "plain",
      "required": true
    }
  }
}
